The speed ledger. One row block per milestone, appended, never rewritten.
A milestone without a number here is not done. Run tools/vkbench.py in the
guest (root), copy the rows, note date / commit / box. Compare against the
block above yours before committing anything: a drop you cannot explain is a
regression you are about to hide in mountains of code.

================================================================
2026-08-10  d497171c  M92p GT730 Win10  (ad-hoc, 2 MB working set)
  CACHE-SCALE numbers -- 2 MB fits L3; kept because they prove the
  window adds no overhead even cache-hot, NOT as bandwidth claims:
  window-write 15034.9 / anon-write 14941.7 MiB/s   (parity)
  window-read  16055.2 / anon-read  16168.7 MiB/s   (parity)

2026-08-10  73e1323f  M92p GT730 Win10  (ad-hoc, pre-vkbench)
  transfer-to-host      738.2 MiB/s   (16 MB tex x16, TRANSFER+WAIT)
  guest-blob create: 16 MB / 18 iovecs OK; mapped+written
  glxgears (X, CoPresent): 679 FPS -- GL path unregressed

2026-08-10  73e1323f  M92p GT730 Win10  (vkbench, 256 MB total, DRAM scale)
  anon-write           4439.4 MiB/s   <- the box's memory bus, the ceiling
  anon-read            4933.1 MiB/s
  blob-write           4916.2 MiB/s   <- PARITY with anon: mapping is free
  blob-read            5012.8 MiB/s
  transfer-to-host     1029.4 MiB/s   <- staged path: ~4.8x under the blob
  Lesson recorded: the first ledger row compared cache-scale to DRAM-scale
  and read as 20x. Honest gap is ~4.8x. The instrument caught its author.

2026-08-10  9aa0e2a6  M92p GT730 Win10  (native control, host python)
  native memmove       4667.6 MiB/s   <- confirms the ceiling: guest range
                                         4439-4933 brackets it; the guest
                                         copies at native speed
  native 2MB cache     9957.0 MiB/s

2026-08-10  952f7caa  M92p GT730 Win10  (same rambench.py, run IN GUEST)
  guest memmove        4865.7 MiB/s   <- native 4667.6: parity, DRAM scale
  guest 2MB cache     10081.2 MiB/s   <- native 9957.0: parity, cache scale
  Verdict: guest == host within ~1% at BOTH scales with one instrument.
  The 15 GiB/s guest row above used a different instrument (mmap slice
  assignment, hotter glibc path than ctypes.memmove) -- an instrument
  difference, not a machine one. Rule: rows are only comparable when the
  code that produced them is the same. Second catch of the evening.

2026-08-10  e4270515  M92p GT730 Win10  (Venus renderer live, GL gate)
  glxgears x4: 579 / 707 / 721 / 605 FPS -- pre-Venus 679 sits mid-band.
  Single gears runs vary ~10% (first run after boot is the low tail);
  never compare single runs. GL unregressed under the Venus DLL + the
  async-fence rework. "venus: up", capset id 4 = v0/160 bytes on GT730.

2026-08-11  25ffffe1  M92p GT730 Win10  (VENUS ENUMERATES; vkbench-venus)
  vulkaninfo: "Virtio-GPU Venus (NVIDIA GeForce GT 730)", DISCRETE_GPU.
  vkMapMemory 64 MB, NV memory type 9 (HOST_VISIBLE|COHERENT):
  map-write            4619.0 MiB/s   <- DRAM ceiling (native 4668): the
                                         zero-copy claim holds for REAL
                                         Vulkan memory end to end
  map-read              223.4 MiB/s   <- NV backs type 9 with WC pages;
                                         reads this slow are native-
                                         correct, apps never read maps
  submit-rtt           1428.4 us      <- empty submit+fence round trip;
                                         OPTIMIZATION TARGET (completion
                                         path: vkr signal -> queue ->
                                         publish -> doorbell). Native is
                                         tens of us. Watch this number.
  GL gate: gears 608 FPS, in band.
================================================================
